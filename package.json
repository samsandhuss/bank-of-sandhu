{
  "name": "bank-of-sandhu",
  "version": "0.1.0",
  "private": true,
  "description": "White-label dual-market digital bank prototype (UK + UAE, neutral + Islamic mode).",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n \"bff,web\" -c \"blue,green\" \"npm:dev:bff\" \"npm:dev:web\"",
    "dev:bff": "node --watch server/index.js",
    "dev:web": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .js,.jsx --max-warnings 0 || echo 'lint stub: configure eslint when ready'",
    "typecheck": "echo 'typecheck stub: add TypeScript when ready'",
    "test": "echo 'test stub: add vitest when ready' && exit 0",
    "validate:brands": "node scripts/validate-brands.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^9.0.1",
    "vite": "^5.4.0"
  }
}
